Apollo SDK:
  feature:
    - Support including humans when launching to the moon
    - Better probability of successful landing
    - internal
  bug-fix:
    - Avoid consuming all oxygen during takeoff
    - Remove kitchen to accommodate space for PlayStation
"@applitools/eyes-universal":
  feature:
    - Support pageCoverage info in Native
  bug-fix:
    - 
"@applitools/eyes-selenium":
  feature:
    - Support pageCoverage info in Native
    - >
      `runner.getAllTestResults` returns the corresponding UFG browser/device
      configuration for each test. This is available as
      `runner.getAllTestResults()[i].browserInfo`.
  bug-fix:
    - 
"@applitools/eyes.webdriverio":
  feature:
    - Support pageCoverage info in Native
    - >
      `runner.getAllTestResults` returns the corresponding UFG browser/device
      configuration for each test. This is available as
      `runner.getAllTestResults()[i].browserInfo`.
  bug-fix:
    - |
      `runner.getAllTestResults` now aborts unclosed tests
    - >
      `runner.getAllTestResults` now returns all results, including aborted
      tests
"@applitools/eyes-playwright":
  feature:
    - Support pageCoverage info in Native
    - >
      `runner.getAllTestResults` returns the corresponding UFG browser/device
      configuration for each test. This is available as
      `runner.getAllTestResults()[i].browserInfo`.
  bug-fix:
    - |
      `runner.getAllTestResults` now aborts unclosed tests
    - >
      `runner.getAllTestResults` now returns all results, including aborted
      tests
"@applitools/eyes-protractor":
  feature:
    - Support pageCoverage info in Native
    - >
      `runner.getAllTestResults` returns the corresponding UFG browser/device
      configuration for each test. This is available as
      `runner.getAllTestResults()[i].browserInfo`.
  bug-fix:
    - |
      `runner.getAllTestResults` now aborts unclosed tests
    - >
      `runner.getAllTestResults` now returns all results, including aborted
      tests
"@applitools/eyes-puppeteer":
  feature:
    - Support pageCoverage info in Native
    - >
      `runner.getAllTestResults` returns the corresponding UFG browser/device
      configuration for each test. This is available as
      `runner.getAllTestResults()[i].browserInfo`.
  bug-fix:
    - |
      `runner.getAllTestResults` now aborts unclosed tests
    - >
      `runner.getAllTestResults` now returns all results, including aborted
      tests
"@applitools/eyes-testcafe":
  feature:
    - Support pageCoverage info in Native
    - >
      `runner.getAllTestResults` returns the corresponding UFG browser/device
      configuration for each test. This is available as
      `runner.getAllTestResults()[i].browserInfo`.
  bug-fix:
    - |
      `runner.getAllTestResults` now aborts unclosed tests
    - >
      `runner.getAllTestResults` now returns all results, including aborted
      tests
"@applitools/eyes-nightwatch":
  feature:
    - Support pageCoverage info in Native
    - >
      `runner.getAllTestResults` returns the corresponding UFG browser/device
      configuration for each test. This is available as
      `runner.getAllTestResults()[i].browserInfo`.
  bug-fix:
    - |
      `runner.getAllTestResults` now aborts unclosed tests
    - >
      `runner.getAllTestResults` now returns all results, including aborted
      tests
"@applitools/eyes-cypress":
  feature:
    - 
  bug-fix:
    - 
"@applitools/eyes-storybook":
  feature:
    - 
  bug-fix:
    - 
"@applitools/eyes-webdriverio5-service":
  feature:
    - 
  bug-fix:
    - 
eyes-browser-extension:
  feature:
    - 
  bug-fix:
    - 
applitools-for-selenium-ide:
  feature:
    - 
  bug-fix:
    - 
